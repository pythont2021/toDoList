<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>days</title>
  </head>
  <body>

    <% if (thisDay === "Saturday" || thisDay === "Sunday"){ %>

      <h2 style = "color:green"> It's a <%= thisDay %> !! </h2>

    <% }else{ %>

      <h2 style = "color:orange"> It's a <%= thisDay %> !! </h2>

    <% } %>

  </body>
</html>
